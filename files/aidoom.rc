/* aiDoom application resources.
   Icon id 1 (the lowest id) is what Explorer and the taskbar use for aidoom.exe.
   The live window icon is set at runtime via SDL_SetWindowIcon (i_video.c). */
1 ICON "../aidoom.ico"
